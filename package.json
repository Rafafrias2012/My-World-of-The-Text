{
  "name": "downloads",
  "version": "1.0.0",
  "description": "Unfortunately Eagrace Max cannot run on a normal Windows 10 computer (other then one in native chinese), so to fix this you'll need to download Locale Emulator in order to get it to work.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@ngrok/ngrok": "^1.3.0"
  }
}
